// Copy this file to supabase-config.ts and set your own Supabase project
// values for local development (`ng serve`). In CI, supabase-config.ts is
// generated automatically from the SUPABASE_URL / SUPABASE_ANON_KEY
// repository variables before the production build (see
// .github/workflows/deploy-pages.yml). The anon key is safe to expose
// publicly; access is restricted by the RLS policies on the `games` table.
export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
